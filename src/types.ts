export interface CONFIG {
  name: string;
  origin: string;
  basePathname: string;
  trailingSlash: boolean;
  title: string;
  description: string;
  defaultImage: string;
  defaultTheme: string;
}
